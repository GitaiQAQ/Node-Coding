// Generated by CoffeeScript 1.10.0
(function() {
  var BaseModel, Historys, util,
    extend = function(child, parent) { for (var key in parent) { if (hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; },
    hasProp = {}.hasOwnProperty;

  BaseModel = require('../BaseModel');

  util = require('util');

  Historys = (function(superClass) {
    extend(Historys, superClass);

    function Historys() {
      return Historys.__super__.constructor.apply(this, arguments);
    }

    return Historys;

  })(BaseModel);

  ({

    /*
    
     method			: get
     summary 		: get
     description	: 获取代码的历史
     header 		: userAgent,accept
     path			: user,project,commits
     query			: page,pageSize
     body			: currentApp,ref,path
     */
    get: (function(_this) {
      return function(user, project, commits, params, fn) {
        if (params == null) {
          params = {};
        }
        if (fn == null) {
          fn = null;
        }
        _this.debug("Historys::get()");
        return _this.get("user/" + user + "/project/" + project + "/git/commits/" + commits, params, function(data) {
          if (fn) {
            return fn(data);
          }
        });
      };
    })(this)
  });

  module.exports = function(client) {
    return new Historys(client);
  };

}).call(this);
