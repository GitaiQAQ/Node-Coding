// Generated by CoffeeScript 1.10.0
(function() {
  var BaseModel, PullRequests, util,
    extend = function(child, parent) { for (var key in parent) { if (hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; },
    hasProp = {}.hasOwnProperty;

  BaseModel = require('../BaseModel');

  util = require('util');

  PullRequests = (function(superClass) {
    extend(PullRequests, superClass);

    function PullRequests() {
      return PullRequests.__super__.constructor.apply(this, arguments);
    }

    return PullRequests;

  })(BaseModel);

  ({

    /*
    
     method			: get
     summary 		: create
     description	: 创建 PullRequest
     path			: user,project
     query			: src_branch,des_user_name,des_project_name,des_branch,diff
     */
    create: (function(_this) {
      return function(user, project, params, fn) {
        if (params == null) {
          params = {};
        }
        if (fn == null) {
          fn = null;
        }
        _this.debug("PullRequests::create()");
        return _this.get("user/" + user + "/project/" + project + "/git/pull/create", params, function(data) {
          if (fn) {
            return fn(data);
          }
        });
      };
    })(this),

    /*
    
     method			: get
     summary 		: get
     description	: 获取某个 PullRequest
     path			: user,project,iid
     query			: diff
     */
    get: (function(_this) {
      return function(user, project, iid, params, fn) {
        if (params == null) {
          params = {};
        }
        if (fn == null) {
          fn = null;
        }
        _this.debug("PullRequests::get()");
        return _this.get("user/" + user + "/project/" + project + "/git/pull/" + iid, params, function(data) {
          if (fn) {
            return fn(data);
          }
        });
      };
    })(this),

    /*
    
     method			: post
     summary 		: cancle
     description	: 取消 PullRequest
     path			: user,project,iid
     */
    cancle: (function(_this) {
      return function(user, project, iid, fn) {
        if (fn == null) {
          fn = null;
        }
        _this.debug("PullRequests::cancle()");
        return _this.post("user/" + user + "/project/" + project + "/git/pull/" + iid + "/cancel", function(data) {
          if (fn) {
            return fn(data);
          }
        });
      };
    })(this),

    /*
    
     method			: get
     summary 		: comments
     description	: PullRequest 评论列表
     path			: user,project,iid
     */
    comments: (function(_this) {
      return function(user, project, iid, fn) {
        if (fn == null) {
          fn = null;
        }
        _this.debug("PullRequests::comments()");
        return _this.get("user/" + user + "/project/" + project + "/git/pull/" + iid + "/comments", function(data) {
          if (fn) {
            return fn(data);
          }
        });
      };
    })(this),

    /*
    
     method			: get
     summary 		: commits
     description	: 获取某个 PullRequest 的评论
     path			: user,project,iid
     */
    commits: (function(_this) {
      return function(user, project, iid, fn) {
        if (fn == null) {
          fn = null;
        }
        _this.debug("PullRequests::commits()");
        return _this.get("user/" + user + "/project/" + project + "/git/pull/" + iid + "/commits", function(data) {
          if (fn) {
            return fn(data);
          }
        });
      };
    })(this),

    /*
    
     method			: post
     summary 		: merge
     description	: 合并 PullRequest
     path			: user,project,iid
     query			: message
     */
    merge: (function(_this) {
      return function(user, project, iid, params, fn) {
        if (params == null) {
          params = {};
        }
        if (fn == null) {
          fn = null;
        }
        _this.debug("PullRequests::merge()");
        return _this.post("user/" + user + "/project/" + project + "/git/pull/" + iid + "/merge", params, function(data) {
          if (fn) {
            return fn(data);
          }
        });
      };
    })(this),

    /*
    
     method			: post
     summary 		: refuse
     description	: 拒绝 PullRequest
     path			: user,project,iid
     */
    refuse: (function(_this) {
      return function(user, project, iid, fn) {
        if (fn == null) {
          fn = null;
        }
        _this.debug("PullRequests::refuse()");
        return _this.post("user/" + user + "/project/" + project + "/git/pull/" + iid + "/refuse", function(data) {
          if (fn) {
            return fn(data);
          }
        });
      };
    })(this),

    /*
    
     method			: get
     summary 		: list
     description	: PullRequest 列表
     path			: user,project,status
     query			: page,pageSize
     */
    list: (function(_this) {
      return function(user, project, status, params, fn) {
        if (params == null) {
          params = {};
        }
        if (fn == null) {
          fn = null;
        }
        _this.debug("PullRequests::list()");
        return _this.get("user/" + user + "/project/" + project + "/git/pulls/" + status, params, function(data) {
          if (fn) {
            return fn(data);
          }
        });
      };
    })(this)
  });

  module.exports = function(client) {
    return new PullRequests(client);
  };

}).call(this);
