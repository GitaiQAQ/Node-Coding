// Generated by CoffeeScript 1.10.0
(function() {
  var BaseModel, Blobs, util,
    extend = function(child, parent) { for (var key in parent) { if (hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; },
    hasProp = {}.hasOwnProperty;

  BaseModel = require('../BaseModel');

  util = require('util');

  Blobs = (function(superClass) {
    extend(Blobs, superClass);

    function Blobs() {
      return Blobs.__super__.constructor.apply(this, arguments);
    }

    return Blobs;

  })(BaseModel);

  ({

    /*
    
     method			: get
     summary 		: get
     description	: 获取 blob
     header 		: userAgent,accept
     path			: user,project,blob
     body			: currentApp,ref,path
     */
    get: (function(_this) {
      return function(user, project, blob, params, fn) {
        if (params == null) {
          params = {};
        }
        if (fn == null) {
          fn = null;
        }
        _this.debug("Blobs::get()");
        return _this.get("user/" + user + "/project/" + project + "/git/blob/" + blob, params, function(data) {
          if (fn) {
            return fn(data);
          }
        });
      };
    })(this)
  });

  module.exports = function(client) {
    return new Blobs(client);
  };

}).call(this);
